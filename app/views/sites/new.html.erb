<% content_for :title, "Add a site — SnagRadar" %>
<div class="max-w-2xl mx-auto">
  <h1 class="font-display text-3xl font-extrabold tracking-tight text-talent-navy">Add a site</h1>
  <div class="mt-6 rounded-2xl bg-white border border-talent-navy/5 shadow-soft p-6 sm:p-8">
    <%= render "form", site: @site %>
  </div>
</div>
